
script(src='http://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js')
script(src='js/frontend.js')
link(rel='stylesheet' href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.0/css/bootstrap.min.css")
link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.0/css/bootstrap-theme.min.css")
script(src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.0/js/bootstrap.min.js")

br
center
  h1 Torch Light
  h4 Tor Network Status & API
  br
  h3 Current # of Onion Routing Sockets:
  h3#node-count #{connections.length}
  br
  h3 The current concensus method is #{connections.length}
  h3 The network consensus began at #{consensus.validAfterDate.format("dddd, MMMM Do YYYY, h:mm:ss a")}.
  h3 The network consensus is fresh until #{consensus.freshUntilDate.format("dddd, MMMM Do YYYY, h:mm:ss a")}.
  h3 The network consensus is valid until #{consensus.validUntilDate.format("dddd, MMMM Do YYYY, h:mm:ss a")}.
  br
  h3 The server version list is #{consensus.serverVersionList}
  h3 The known routers flags are #{consensus.flagsString()}.
  br
  h3 There are #{consensus.routers.length} routers currently in the network concensus.
  br
  p
    table.table-striped.table-bordered
        tr
            th #
            th Name
            th Location
            th IP
        each router, index in consensus.routers
            tr
                td= index
                td= router.name
                //- if router.geo.city
                    //- td= router.geoString
                //- else
                td= router.geo.country
                td= router.IP
  
  h4
    a(href="/raw") JSON
    br
    br
    a(href="http://github.com/sheldonth/torchlight") Source
    
    
